/**
 * The one place the product name lives in JS. The CSS renders it from
 * --brand-name; anything that needs it in script reads it from here.
 */
export const BRAND_NAME = 'Factory Floor'

/** The group dashboard that owns accounts and passwords. The one external link. */
export const ACCOUNTS_URL = 'https://agarwalgabions.com/dashboard'
